# Battery
CONFIG_ZMK_BATTERY_REPORT_INTERVAL=60
# Debounce
CONFIG_ZMK_KSCAN_DEBOUNCE_PRESS_MS=5
CONFIG_ZMK_KSCAN_DEBOUNCE_RELEASE_MS=5

# =============================================================================
# BLUETOOTH & CONNECTION SETTINGS
# =============================================================================
# BLE TX power
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y
# Fix Windows battery bug
CONFIG_BT_GATT_ENFORCE_SUBSCRIPTION=n
# Split central: 5 host profiles + 1 split peripheral link
CONFIG_BT_MAX_CONN=3
CONFIG_BT_MAX_PAIRED=3
# Improve BLE reconnection stability (disables 2M PHY)
CONFIG_ZMK_BLE_EXPERIMENTAL_CONN=y
# Additional BLE stability settings
CONFIG_BT_GATT_CLIENT=y
CONFIG_BT_PERIPHERAL=y
CONFIG_BT_CENTRAL=y
CONFIG_BT_SMP=y
# Split battery level monitoring
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_PROXY=y
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_FETCHING=y
# Output status widget
CONFIG_ZMK_WIDGET_OUTPUT_STATUS=y

# =============================================================================
# DISPLAY & OLED SETTINGS
# =============================================================================
# OLED
CONFIG_ZMK_DISPLAY=y
CONFIG_ZMK_DISPLAY_WORK_QUEUE_DEDICATED=y
CONFIG_ZMK_DISPLAY_STATUS_SCREEN_BUILT_IN=y

# =============================================================================
# POWER & SLEEP SETTINGS
# =============================================================================
CONFIG_ZMK_IDLE_TIMEOUT=1800000
CONFIG_ZMK_SLEEP=n

# =============================================================================
# INPUT & TRACKPAD SETTINGS
# =============================================================================
# Pointing (for trackpad on right half)
CONFIG_ZMK_POINTING=y

# =============================================================================
# DEVELOPMENT & DEBUGGING
# =============================================================================
# ZMK Studio
CONFIG_ZMK_STUDIO=y
CONFIG_ZMK_STUDIO_LOCKING=n
CONFIG_ZMK_STUDIO_LOCK_ON_DISCONNECT=n
